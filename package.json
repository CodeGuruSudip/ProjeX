{
  "name": "project-management-tool",
  "version": "1.0.0",
  "description": "A comprehensive project management tool with features like task management, team collaboration, and project tracking.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "cross-env PORT=5000 nodemon server.js",
    "client": "npm start --prefix frontend",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "npm install && cd frontend && npm install && npm run build"
  },
  "author": "Cascade",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "colors": "^1.4.0",
    "concurrently": "^7.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.5.0",
    "multer": "^1.4.5-lts.1",
    "nodemon": "^2.0.15"
  },
  "devDependencies": {
    "cross-env": "^10.0.0"
  }
}
